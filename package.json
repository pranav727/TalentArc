{
  "name": "talentarc",
  "version": "1.0.0",
  "description": "World-Class AI Job Board Platform",
  "main": "index.html",
  "scripts": {
    "dev": "npx http-server -p 3000 -c-1",
    "build": "echo 'Static site — no build step required' && exit 0",
    "test": "echo 'No automated tests yet' && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}